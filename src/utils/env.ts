// eslint-disable-next-line import/prefer-default-export
export const ENV = {
  backendHost: 'oscar.db.docker', // used in ssr api calling in docker
  cookiePrefix: 'symplicit',
  debugger: false,
  graphqlHost: 'localhost',
  graphqlPort: 3003,
  restApiPort: 3002,
  port: 4000,
};
